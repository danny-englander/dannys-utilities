# OpenSea Scraper

A tool to scrape an OpenSea collection's metadata and images. This is useful for getting the metadata and images for a collection in a single JSON file.

## Usage

```bash
python opensea-scraper.py --api-key YOUR_API_KEY
```

## Output

The output will be a JSON file in the current directory.
